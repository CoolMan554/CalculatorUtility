syntax = "proto3";

package calculator;

message CalcRequest {
    double arg1 = 1;
    double arg2 = 2;
    string operation = 3;
}

message CalcResponse {
    double result = 1;
}

service CalculatorService {
    rpc Compute(CalcRequest) returns (CalcResponse);
}